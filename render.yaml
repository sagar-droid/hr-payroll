services:
  - type: web
    name: hr-payroll-api
    runtime: node
    buildCommand: npm ci && npm run build --workspace=apps/api
    startCommand: node apps/api/dist/index.js
    envVars:
      - key: NODE_ENV
        value: production
